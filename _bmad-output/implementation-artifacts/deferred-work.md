# Deferred Work

## Deferred from: code review of 1-1-audit-konnect-provider-3-15-schema-diffs-against-current-resource-usage (2026-05-08)

- **(MED)** `modules/cloud_gateway_configuration/main.tf:1-7` and `modules/control_plane/main.tf:1-7` declare `kong/konnect` source without a `version` pin — they inherit from root. Pre-existing inheritance, not introduced by Story 1.1; flag for Story 1.2 / 1.3 to either pin explicitly or accept inheritance deliberately.
- **(MED)** `konnect_audit_log_destination` authorization is `lookup(..., null)` for a Required attribute (audit mentions it in passing as "matches 3.1.0 behavior"). Same wiring-passes-null-for-required pattern flagged for `system_account`. Consider a unified watchpoint section in a future audit pass. Pre-existing wiring, not a Story 1.1 deliverable concern.
- **(LOW)** `konnect_centralized_consumer` module conditionally emits `consumer_groups`/`tags` based on `length() > 0`; audit's flat attribute list hides this. Matters only if 3.4.2's null-handling change (sets to `null`) affects state. Pre-existing module behavior.
- **(LOW)** `konnect_api` `spec_content` is `Requires replacement if changed` per 3.15 schema; audit's `no-change` classification is correct on the schema-diff axis but doesn't flag the ForceNew implication for Story 1.6's clean-plan exercise. Pre-existing schema fact.
- **(LOW)** `konnect_system_account_role` outer-tree "Count: 8" conflates 8 blocks with 0–8 actual instances (each `count`-guarded by entitlement). Affects state-address claims if entitlements differ between teams. Cosmetic.
- **(LOW)** `konnect_portal_auth` per-resource section's "Recommended Story 1.6 / MIGRATION.md §2 treatment" subsection is forward-prescriptive — borderline against the spec's "does not touch MIGRATION.md" rule. Reframe as "Watchpoint for Story 1.6" to match the cloud_gateway_configuration section style.

## Deferred from: code review of 1-2-bump-konnect-provider-to-3-15-and-apply-schema-updates-in-terraform-konnect-teams (2026-05-08)

- **(HIGH → defer per spec D1)** [terraform/konnect-teams/modules/system-account/main.tf:1-7](terraform/konnect-teams/modules/system-account/main.tf#L1-L7) declares `kong/konnect` source without a `version` constraint. Inheritance from root works only when `init` is run from the root; running `cd modules/system-account && terraform init` standalone will resolve whatever the registry currently labels latest (today 3.15.x; tomorrow possibly 3.16+). Spec deliberately accepted inheritance — revisit in a future hardening pass; consider either an explicit pin or a comment forbidding direct submodule init.
- **(HIGH → repo-wide policy)** `.terraform.lock.hcl` is gitignored at [.gitignore:40](.gitignore#L40). Even with an exact `=` pin, transitive provider hashes are not locked across operators/platforms, and the workflow's `init -upgrade` would override a committed lockfile anyway. Pre-existing repo policy; surface for Story 1.6 to decide whether to commit the lockfile and drop `-upgrade`, or keep status quo deliberately.
- **(HIGH → Story 1.3 territory)** Inner tree at [.github/actions/provision-konnect-resources/terraform/main.tf:5](.github/actions/provision-konnect-resources/terraform/main.tf#L5) and ~20 submodules under `modules/*/main.tf:5` still pin `kong/konnect = 3.1.0`. Until Story 1.3 lands, the repo runs two major versions of the same provider. Acceptable per spec scope; flagged so a release/tag is not cut at this point. README at `.github/actions/provision-konnect-resources/terraform/README.md:57` likewise still says "v3.1.0" — Story 1.3 should sweep both.

## Deferred from: code review of 1-3-bump-konnect-provider-to-3-15-and-apply-schema-updates-in-github-actions-provision-konnect-resources-terraform (2026-05-09)

- **(MED → schema rot uncovered)** [`modules/api_implementation/main.tf:14-17`](.github/actions/provision-konnect-resources/terraform/modules/api_implementation/main.tf#L14-L17) sets `service = { control_plane_id, id }` as a top-level argument on `konnect_api_implementation`, but the resource attribute `service` is `computed` (a read-only output) — the input shape required by 3.15.0 is `service_reference = { service = { control_plane_id, id } }`. Module is commented out in [`terraform/main.tf:692-702`](.github/actions/provision-konnect-resources/terraform/main.tf#L692-L702), so the rot has been masked since the schema cache was generated. Surfaced during Story 1.3 code review when adding a `variables.tf` made the body validate independently. Resolution requires rewriting the resource body and authoring a `variables.tf` — feature work outside the bump scope. Candidate for Story 1.6 (clean-plan gate) or a dedicated Epic-2 cleanup.
- **(LOW → schema cache)** [`.github/actions/provision-konnect-resources/terraform/schema.json`](.github/actions/provision-konnect-resources/terraform/schema.json) is the bundled `kong/konnect` schema cache; it remains at the 3.1.0 shape after the bump. Per Story 1.3 AC7 deliberately not regenerated. No active consumer found in repo (grep against `.github/`, `scripts/`, `*.sh|*.yaml|*.tf|*.py|*.js`). Surface for end-of-Epic-1 cleanup; regenerate via `terraform providers schema -json` once the bump has soaked.
- **(MED → AC5 plan brittleness)** [`.github/actions/provision-konnect-resources/terraform/main.tf:95`](.github/actions/provision-konnect-resources/terraform/main.tf#L95) `data "terracurl_request" "fetch_team"` runs unconditionally during plan, unlike its sibling `fetch_user_by_email` at line 113 which carries `for_each = var.konnect_access_token == "dummy" ? {} : ...`. Pre-existing structural issue exposed during Story 1.3's AC5 dummy-token plan; not a 3.15.0 regression. Adding the same dummy guard to `fetch_team` would make refresh-less plan locally executable without a live token. Candidate for Story 1.6 if the MinIO gate also wants a dummy-token branch, or for a standalone Epic-2 cleanup story.
